---
// Portfolio Index Page
// ------------
// Description: The portfolio index page.

// Components
// - Layout
import Layout from '../../layouts/Layout.astro'
// - UI
import Hero from '../../components/blocks/hero/PageHeader.astro'
import PortfolioPosts from '../../components/blocks/portfolio/PortfolioPosts.astro'

// Data
import { getCollection } from 'astro:content'

// Content
// - SEO
const SEO = {
	title: 'Agency | Latest Agency News and updates',
	description:
		"Explore Agency's latest news designed to enhance productivity, ensure top-notch security, and integrate seamlessly with your favorite tools. Join thousands of satisfied users and transform your workflow with Agency today!"
}
// - Page Header
const header = {
	title: 'The <strong>Agency</strong> Portfolio. Tips, Updates & Stories',
	text: 'Stay informed, stay productive. All the latest from the world of Agency.'
}

const allPosts = await getCollection('portfolio')
---

<Layout title={SEO.title} description={SEO.description}>
	<Hero title={header.title} text={header.text} />
	<PortfolioPosts data={allPosts} />
</Layout>
